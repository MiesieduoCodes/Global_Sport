/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'instagram.flos5-2.fna.fbcdn.net',
    ],
  }
};

export default nextConfig;
